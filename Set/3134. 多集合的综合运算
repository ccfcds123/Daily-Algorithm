/*
题目描述
给定 
K
K 个由整数组成的序列，请你先将这 
K
K 个序列分别去重，视为 
K
K 个集合：
S
1
,
S
2
,
…
,
S
K
S 
1
​
 ,S 
2
​
 ,…,S 
K
​
 。

请你计算并输出以下两个结果：

这 
K
K 个集合的总交集 
⋂
i
=
1
K
S
i
⋂ 
i=1
K
​
 S 
i
​
  （即在所有 
K
K 个集合中都出现过的元素）。
这 
K
K 个集合的总并集 
⋃
i
=
1
K
S
i
⋃ 
i=1
K
​
 S 
i
​
  （即至少在一个集合中出现过的元素）。
要求与限制：

所有输出的集合元素必须按 从小到大（升序）排列。
禁止使用任何STL库。
若某次操作得到的集合为空，请输出 EMPTY。
输入格式
第一行包含一个正整数 
K
K，表示序列的数量。接下来有 
K
K 个部分，每个部分包含两行：

第一行是一个整数 
N
i
N 
i
​
 ，表示第 
i
i 个序列的元素个数。
第二行是 
N
i
N 
i
​
  个整数，表示第 
i
i 个序列的具体元素，相邻元素由空格隔开。（注：若 
N
i
=
0
N 
i
​
 =0，则第二行将为空行）。
输出格式
输出共两行：

第一行：总交集的元素，用空格隔开。若为空，输出 EMPTY。
第二行：总并集的元素，用空格隔开。若为空，输出 EMPTY。
样例输入
3
5
1 2 2 3 4
4
2 3 5 5
6
0 2 3 3 8 9
样例输出
2 3
0 1 2 3 4 5 8 9
去重后，三个集合分别为：

S
1
=
1
,
2
,
3
,
4
S 
1
​
 =1,2,3,4

S
2
=
2
,
3
,
5
S 
2
​
 =2,3,5

S
3
=
0
,
2
,
3
,
8
,
9
S 
3
​
 =0,2,3,8,9

三个集合共同拥有的元素（总交集）是2 和3。

所有出现过的元素去重后（总并集）是0, 1, 2, 3, 4, 5, 8, 9。

数据范围
对于30% 的数据，
1
≤
K
≤
3
1≤K≤3，
∑
N
i
≤
1000
∑N 
i
​
 ≤1000。
对于100% 的数据，
1
≤
K
≤
100
1≤K≤100，所有序列长度之和 
∑
N
i
≤
2
×
1
0
5
∑N 
i
​
 ≤2×10 
5
 ，序列中每个元素的值 
V
V 满足 
−
1
0
9
≤
V
≤
1
0
9
−10 
9
 ≤V≤10 
9
 。
时空磁盘限制（运行时）
时间限制：1000 ms

内存空间限制：64 MiB

磁盘空间限制：不可使用磁盘

单个测试点时空限制详情
*/

#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

vector<int> unique_sort(vector<int>& arr) {
    if(arr.empty()) return {};
    sort(arr.begin(), arr.end());
    vector<int> result;
    result.push_back(arr[0]);
    for(int i = 1; i < arr.size(); i++) {
        if(arr[i] != arr[i-1]) {
            result.push_back(arr[i]);
        }
    }
    return result;
}

vector<int> union_sorted(vector<int>& arr1, vector<int>& arr2) {
    vector<int> result;
    int i = 0, j = 0;
    while(i < arr1.size() && j < arr2.size()) {
        if(arr1[i] < arr2[j]) {
            result.push_back(arr1[i]);
            i++;
        } else if(arr1[i] > arr2[j]) {
            result.push_back(arr2[j]);
            j++;
        } else {
            result.push_back(arr1[i]);
            i++;
            j++;
        }
    }
    while(i < arr1.size()) {
        result.push_back(arr1[i]);
        i++;
    }
    while(j < arr2.size()) {
        result.push_back(arr2[j]);
        j++;
    }
    return result;
}

vector<int> intersection_sorted(vector<int>& arr1, vector<int>& arr2) {
    vector<int> result;
    int i = 0, j = 0;
    while(i < arr1.size() && j < arr2.size()) {
        if(arr1[i] < arr2[j]) {
            i++;
        } else if(arr1[i] > arr2[j]) {
            j++;
        } else {
            result.push_back(arr1[i]);
            i++;
            j++;
        }
    }
    return result;
}



int main() {
    int K;
    cin >> K;
    vector<vector<int>> arr(K);
    for(int i = 0; i < K; i++) {
        int N;
        cin >> N;
        vector<int> temp(N);
        for(int j = 0; j < N; j++) {
            cin >> temp[j];
        }
        arr[i] = temp;
    }
    vector<vector<int>> unique_arr(K);
    for(int i = 0; i < K; i++) {
        unique_arr[i] = unique_sort(arr[i]);
    }

    vector<int> union_result;
    if(unique_arr[0].size()) {
        union_result = unique_arr[0];
        for(int i = 1; i < K; i++) {
            union_result = union_sorted(union_result, unique_arr[i]);
        }
    }
    vector<int> intersection_result;
    if(unique_arr[0].size()) {
        intersection_result = unique_arr[0];
        for(int i = 1; i < K; i++) {
            intersection_result = intersection_sorted(intersection_result, unique_arr[i]);
        }
    }

    if(!intersection_result.size()) cout << "EMPTY";
    for(int num : intersection_result) {
        cout << num << " ";
    }
    cout << endl;
    if(!union_result.size()) cout << "EMPTY";
    for(int num : union_result) {
        cout << num << " ";
    }

    return 0;
}
